extends layout

block content
  h2 Organisations
  ul#organizations-list.list-group
    if organizations
      each organization in organizations
        li.list-group-item= organization.name
    else
      li.list-group-item not fetching list


    block content
  style.
    body {
      background-color: #f0f0f0;
      color: green;
      font-size: 40px;
      align: center; 
    }




